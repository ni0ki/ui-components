import * as React from 'react';
import SvgIcon from '@atoms/svg/base/SvgIcon';

const {{pascalCase name}}: React.FunctionComponent<import('../base/SvgIcon').IconProps> = (props: import('../base/SvgIcon').IconProps): JSX.Element => (
<SvgIcon
        title='{{sentenceCase name}}'
        {...props}
><React.Fragment>{{{path}}}</React.Fragment></SvgIcon>
);

export default {{pascalCase name}};
