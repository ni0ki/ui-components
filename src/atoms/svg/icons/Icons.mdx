---
name: Icons
menu: Components
---

import { Playground, Props } from 'docz';
import SvgIcon from '../base/SvgIcon';
import Edit from './Edit';
import Hide from './Hide';
import Reveal from './Reveal';
import NoNetwork from './NoNetwork';
import CopyPassword from './CopyPassword';
import Loader from './Loader';
import Shared from './Shared';
import CaretDown from './CaretDown';
import Check from './Check';
import Vault from './Vault';
import Web from './Web';
import Generator from './Generator';
import Dots from './Dots';
import Search from './Search';
import Close from './Close';
import ArrowDown from './ArrowDown';
import Warning from './Warning';
import Refresh from './Refresh';
import Tools from './Tools';
import Computer from './Computer';
import LogOut from './LogOut';
import PasswordChanger from './PasswordChanger';
import SecurityDashboard from './SecurityDashboard';
import PasswordHistory from './PasswordHistory';
import Passwords from './Passwords';

# Icons

UI-components offers a set of icons ready to be used in a web application. Icons are built as React components using the `SvgIcon` component.

## Properties

Icons can be configured in the same way (have the same props) as the SvgIcon component (expect the `children` prop since the svg content is already set for each icon). Te see theses props in action, you can either go to the documentation of the SvgIcon component or open the playground of the icons and add some props.

<Props of={Edit} />

## Available icons

<Playground
  style={{
    backgroundColor: '#F5F6F7',
    padding: '30px',
    borderRadius: '4px',
    display: 'flex',
    flexDirection: 'row',
    flexWrap: 'wrap',
    justifyContent: 'space-around'
  }}
>
  <Edit title='translation_key' />
  <CopyPassword />
  <Reveal />
  <Hide />
  <NoNetwork />
  <Shared />
  <Loader />
  <CaretDown />
  <Check />
  <Vault />
  <Web />
  <Generator />
  <Dots />
  <Search />
  <Close />
  <ArrowDown />
  <Refresh />
  <Tools />
  <Computer />
  <PasswordChanger />
  <SecurityDashboard />
  <PasswordHistory />
  <Passwords />
</Playground>
